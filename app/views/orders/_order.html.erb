<% cache order do %>
  <tr>
    <td data-sort-value="<%= order.price %>"><%= order.price %>kr</td>
    <td class="quality" data-sort-value="<%= order.quality %>"><%= stars(order.quality) %></td>
    <td><%= link_to 'Buy', [:buy, @item, order], class: 'button' %></td>
  </tr>
<% end %>
