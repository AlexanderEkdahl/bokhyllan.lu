<input type="text"
       id="search"
       placeholder="Search title, author, course, etc."
       value="<%= @item.name if @item %>"
       data-prefetch="<%= typeahead_items_path(format: :json) %>">
